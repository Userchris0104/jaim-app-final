name = "jaim-final"
compatibility_date = "2024-12-01"
compatibility_flags = ["nodejs_compat"]
pages_build_output_dir = "./build/client"

# D1 Database
[[d1_databases]]
binding = "DB"
database_name = "jaim-final-db"
database_id = "30336a5c-3520-4a2f-afde-80fcfe4655d9"

# R2 Storage
[[r2_buckets]]
binding = "R2"
bucket_name = "jaim-final-assets"

# Environment variables
[vars]
ENVIRONMENT = "production"
USE_MOCK_PLATFORMS = "false"  # Set to "true" for local development with mock TikTok/Meta APIs

# Anthropic Claude — ad copy generation
# ANTHROPIC_API_KEY = "your-key-here"
# Get key at: console.anthropic.com

# Google Gemini — PRIMARY scene/ad image generation
# GEMINI_API_KEY = "your-gemini-api-key-here"
# Get your key at: https://aistudio.google.com/apikey
# Model: gemini-3.1-flash-image-preview
# Cost: $0.067/image (standard), $0.034/image (batch)

# OpenAI — product image analysis (GPT-4o-mini vision)
# OPENAI_API_KEY = "your-openai-api-key-here"
# Get your key at: https://platform.openai.com

# ===========================================
# REMOVED FROM PIPELINE
# ===========================================

# FAL_API_KEY — REMOVED
# fal.ai no longer used in pipeline
# Bria RMBG removed (Gemini handles product natively)
# FLUX removed (Gemini is the only image provider)

# PHOTOROOM_API_KEY — REMOVED
# Photoroom no longer used in pipeline
# Gemini handles product integration natively

# FASHNAI_API_KEY — reserved for future
# Model-wearing-product generation (not active)
